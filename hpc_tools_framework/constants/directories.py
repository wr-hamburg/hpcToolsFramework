TOOL_DIR = "hpcToolsFramework"
"""The root directory for the tool which contains all other directories."""

JOBSCRIPT_DIR = "jobscripts"
"""The jobscript directory which contains the generated jobscripts."""

JOBSCRIPT_OUTPUT_DIR = "jobscript_outputs"
"""The jobscript output directory which contains the outputs of the jobscripts."""

JOBSCRIPT_CHECKPOINT_DIR = "jobscript_checkpoints"
"""The jobscript checkpoint directory."""

OUTPUT_DIR = "outputs"
"""The output directory which contains the the analysis tool outputs."""

OUTPUT_LOCAL_DIR = ".\hpcToolsFramework\outputs"
"""The directory which contains all formated PDF outputs of all tools on the local harddrive."""

INPUT_DIR = "inputs"
"""The input directory for files like filter or selection files in TAU or Score-P or the Extrae config file."""

DATABASE_DIR = "database"
"""The database directory."""

REPORT_DIR = "reports"
"""The reports directory which contains the generated pdf reports for each finished job."""